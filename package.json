{
  "name": "smjs",
  "version": "0.0.0",
  "description": "Sloth Machine JS",
  "main": "main.js",
  "repository": "https://github.com/michelezamuner/smjs",
  "author": "Michele Zamuner",
  "license": "MIT",
  "dependencies": {},
  "devDependencies": {
    "jest": "^23.6.0"
  },
  "scripts": {
    "test": "node --use_strict ./node_modules/.bin/jest",
    "coverage": "node --use_strict ./node_modules/.bin/jest --config jest.config.dev.json --coverage $(pwd)/tests",
    "ci": "node --use_strict ./node_modules/.bin/jest --config jest.config.ci.json --coverage $(pwd)/tests"
  }
}
