#!/usr/bin/env sh

readonly current="$( cd "$(dirname "$0")" ; pwd -P )"
readonly root="$(realpath "$current/../../../")"
docker run -dit --tmpfs /tmp --name sm-apache -p 8080:80 -v ${root}:/usr/local/apache2/cgi-bin/smjs sm-apache
